Vector search engine based on qdrant vector database.

It requires to run standalone qdrant server, which is not convenient.  

Temporarely keeping it as a reference vector search implementation, which eventually will be replaced with `VectorSearchEngine`, based on embedded sqlite vector database.
